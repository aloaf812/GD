#include "MenuGameLayer.h"

/* MenuGameLayer* MenuGameLayer::create() {
    MenuGameLayer* ret = new MenuGameLayer();
    if (ret) {
        if (ret->init()) {
            ret->autorelease();
            return ret;
        }

        delete ret;
    }

    return NULL;
} */